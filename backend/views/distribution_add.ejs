<div class="card shadow-sm">
  <div class="card-header bg-dark text-white">
    <h5><i class="fa fa-plus"></i> Tambah Distribusi</h5>
  </div>
  <div class="card-body">
    <form action="/distribution/add" method="POST">
      <div class="mb-3">
        <label class="form-label">Produk</label>
        <select class="form-select" name="product_id" required>
          <option value="">-- Pilih Produk --</option>
          <% products.forEach(p => { %>
            <option value="<%= p.id %>"><%= p.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3">
        <label class="form-label">Dari Toko</label>
        <select class="form-select" name="from_store_id" required>
          <option value="">-- Pilih Toko --</option>
          <% stores.forEach(s => { %>
            <option value="<%= s.id %>"><%= s.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3">
        <label class="form-label">Ke Toko</label>
        <select class="form-select" name="to_store_id" required>
          <option value="">-- Pilih Toko --</option>
          <% stores.forEach(s => { %>
            <option value="<%= s.id %>"><%= s.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3">
        <label class="form-label">Jumlah</label>
        <input type="number" class="form-control" name="quantity" required>
      </div>

      <button type="submit" class="btn btn-success">
        <i class="fa fa-save"></i> Simpan
      </button>
      <a href="/distribution" class="btn btn-secondary">
        <i class="fa fa-arrow-left"></i> Batal
      </a>
    </form>
  </div>
</div>
