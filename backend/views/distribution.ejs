<div class="d-flex justify-content-between align-items-center mb-3">
  <h2><i class="fa fa-truck"></i> <%= title %></h2>
  <a href="/distribution/add" class="btn btn-primary">
    <i class="fa fa-plus"></i> Tambah Distribusi
  </a>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <table class="table table-striped table-hover align-middle">
      <thead class="table-dark">
        <tr>
          <th>Produk</th>
          <th>Dari Toko</th>
          <th>Ke Toko</th>
          <th>Qty</th>
          <th>Status</th>
          <th>Tanggal</th>
        </tr>
      </thead>
      <tbody>
        <% if (!data || data.length === 0) { %>
          <tr><td colspan="6" class="text-center text-muted">Belum ada distribusi</td></tr>
        <% } else { %>
          <% data.forEach(d => { %>
            <tr>
              <td><%= d.product %></td>
              <td><%= d.from_store %></td>
              <td><%= d.to_store %></td>
              <td><%= d.quantity %></td>
              <td>
                <span class="badge <%= d.status === 'completed' ? 'bg-success' : 'bg-warning text-dark' %>">
                  <%= d.status %>
                </span>
              </td>
              <td><%= new Date(d.distribution_date).toLocaleString("id-ID") %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</div>
